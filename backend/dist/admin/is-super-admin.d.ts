export declare function isSuperAdmin(user: {
    role?: string;
    email?: string;
} | undefined | null): boolean;
export declare function isAdmin(user: {
    role?: string;
    email?: string;
} | undefined | null): boolean;
